CMAKE_MINIMUM_REQUIRED(VERSION 3.1)

INCLUDE_DIRECTORIES(.
        ${CMAKE_SOURCE_DIR}/3rdParty/SDL/include/
        )

AUX_SOURCE_DIRECTORY(. SRC)

ADD_EXECUTABLE(Test_DisPics ${SRC})

TARGET_LINK_LIBRARIES(Test_DisPics ${ThirdParty}/SDL/lib/libSDL2.so)